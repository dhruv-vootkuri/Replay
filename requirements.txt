# Pinned to the exact versions this project is known to work with.
# Prefer `pip install -e ".[demo]"` (see README) — this file exists for
# reproducing the known-good environment exactly.

# --- core engine ---
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-instrumentation-openai==0.53.3
opentelemetry-instrumentation-langchain==0.53.3
openai==2.30.0
litellm==1.82.6
click==8.3.1
blessed==1.34.0
pydantic==2.12.5
python-dotenv==1.2.2

# --- web console ---
fastapi==0.135.2
uvicorn==0.42.0

# --- capturing traces from a LangChain agent (demo_agent.py) ---
langchain==1.2.13
langchain-core==1.2.22
langchain-openai==1.1.12
langgraph==1.1.3
