# Copy to .env and fill in:  cp .env.example .env
#
# Required only for capturing new traces and for replaying/pressure-testing
# (both make real model calls). Browsing traces already on disk needs no key.
OPENAI_API_KEY=sk-...
